import { Typography, Box } from '@mui/material';

const EmployeeHome = () => {
  return (
    <Box p={4}>
      <Typography variant="h4">직원 홈입니다</Typography>
    </Box>
  );
};

export default EmployeeHome;
